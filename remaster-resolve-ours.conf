# dm-remaster-parent: paths resolved toward OURS on a sync-merge conflict with upstream,
# instead of blocking. One path per line; '#' comments and blank lines ignored.
#
# These are files our fork DELIBERATELY REFACTORED away (content relocated), so upstream's
# edits to the old location are superseded and must not block or revert the sync. Keeping
# master mergeable from kicksecure/master's perspective depends on this.
#
# WARNING: taking ours DROPS upstream's change to the path. Review periodically that the
# upstream semantic change is still carried in our new location.

# Minimized into buildconfig.d/*; the old monolithic loader is superseded.
help-steps/variables
