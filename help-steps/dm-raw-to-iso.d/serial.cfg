## Route GRUB output to the serial console (appended to config.cfg under
## --serial-console). 'console' kept so video output still works if serial fails.
insmod serial
serial --unit=0 --speed=115200 --word=8 --parity=no --stop=1
terminal_output serial console
